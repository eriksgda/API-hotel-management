services:
  postgres:
    container_name: "hotel_management_db"
    image: postgres
    ports:
      - "5432:5432"
    environment:
      - "POSTGRES_USER=admin"
      - "POSTGRES_PASSWORD=admin"
      - "POSTGRES_DB=hotel_management"
